{% extends 'base.html.twig' %}
{% block title %}Immo Agency!{% endblock %}
{% block body %}
    <div class="jumbotron text-center">
        <h1>Immo Agency</h1>
        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Ab beatae excepturi pariatur sit voluptas. Blanditiis dicta explicabo, illo iure laboriosam necessitatibus odio omnis porro quasi ullam! Eaque inventore itaque vitae!</p>
    </div>
    <div class="container">
        <h2>Last Properties</h2>
        <div class="row flex">
            {% for property in properties %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                            </h5>
                            <a href="{{ path('property.text') }}"><p class="card-text">{{ property.city }} ({{ property.postalCode }})</p></a>
                            <div class="text-primary" style="font-weight: bold;font-size: 2rem;">{{ property.FormattedPrice }} €</div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}